library testing_container;

import 'package:flutter/material.dart';

class containerColor extends StatelessWidget {
  const containerColor({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
     height: 100,
     width: 100,
     color: Colors.red,
    );
  }
}

